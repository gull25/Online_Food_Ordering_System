const fs = require('fs'); const files = ['RiderDashboard.jsx', 'ActiveDeliveries.jsx', 'Earnings.jsx', 'Ratings.jsx']; files.forEach(f => { let content = fs.readFileSync('client/src/pages/Rider/' + f, 'utf8'); const regex = /<RiderProfileDropdown avatarSrc=\{.*?\} \/>/s; content = content.replace(regex, '<RiderProfileDropdown />'); fs.writeFileSync('client/src/pages/Rider/' + f, content); });
